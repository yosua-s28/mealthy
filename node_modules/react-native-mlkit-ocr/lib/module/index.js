import { NativeModules } from 'react-native'; // https://developers.google.com/ml-kit/reference/android

const MlkitOcr = NativeModules.MlkitOcr;
const MLKit = {
  detectFromUri: async uri => {
    const result = await MlkitOcr.detectFromUri(uri);

    if (!result) {
      return [];
    }

    return result;
  },
  detectFromFile: async path => {
    const result = await MlkitOcr.detectFromFile(path);

    if (!result) {
      return [];
    }

    return result;
  }
};
export default MLKit;
//# sourceMappingURL=index.js.map